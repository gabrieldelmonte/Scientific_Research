[package]
name = "web_server"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = {version = "1.47.1", features = ["full"]}
rusqlite = {version = "0.37.0", features = ["bundled"]}

axum = "0.8.4"
chrono = "0.4.41"
